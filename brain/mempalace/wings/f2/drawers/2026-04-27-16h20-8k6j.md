---
tags: [f2, conversation, 2026-04-27]
date: "2026-04-27"
source: "jarvis-web"
messages: 1
---
# Session f2 — 2026-04-27 16h20 (1 échanges)

[USER 16:17]
alors?

[ASSISTANT 16:17]
Je vois le bug exactement — ligne 66 de `routes/context.ts`. La fonction `section()` s'active sur n'importe quelle ligne qui *contient* "IH", pas seulement les headings `## IH`. Et `appendTableRow()` dans `markdown.ts` a le même problème.

Je check la compilation avant de proposer les patches.Le code compile proprement. Je propose les 2 patches — **1 seule ligne changée dans chaque fichier**.

---

**Bug confirmé :** `section(markdown, "IH")` ligne 66 dans `routes/context.ts` active sur n'importe quelle ligne contenant "IH" — y compris des métadonnées comme `**Plateforme :** IH principalement.` dans le header du log F2. Il s'active là, puis le vrai `## IH` déclenche le `break`. Section retournée = vide → compteur = 0.

Même bug dans `appendTableRow()` de `markdown.ts` — futur `log_engagement` IH aurait le même problème.

---2 patches, 1 ligne changée chacun. Fix définitif — demain matin le compteur IH affichera **10** sans intervention manuelle.

**Patch 1 — `routes/context.ts`** (compteur sidebar)
```diff
- if (!active && line.includes(title)) { active = true; continue; }
+ if (!active && /^#+\s/.test(line) && line.includes(title)) { active = true; continue; }
```
[ACTION_PENDING:4a097e9c-3b1b-45c0-860a-06f6e19080cf]

**Patch 2 — `markdown.ts`** (write future-proof)
```diff
- if (!inSection && line.includes(sectionTitle)) { inSection = true; continue; }
+ if (!inSection && /^#+\s/.test(line) && line.includes(sectionTitle)) { inSection = true; continue; }
```
[ACTION_PENDING:27668a38-0b40-40fb-b792-754511d6b45b]

Valide les 2 et le fix est en prod.

[TAG:Bilan F2 fin de journée] [TAG:10/10 IH confirmé après fix]
