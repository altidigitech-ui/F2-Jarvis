---
tags: [fabrice, conversation, 2026-04-26]
date: "2026-04-26"
source: "jarvis-web"
messages: 1
---
# Session fabrice — 2026-04-26 19h27 (1 échanges)

[USER 19:23]
Voila les 3 fix ont était fait analyse mon repo et dit moi si tout est ok si mon App est prete

[ASSISTANT 19:23]
Je lis les fichiers critiques en parallèle — config, billing, ghost billing, fixer, et main.Je lis maintenant les fichiers restants — shopify.py, main.py, orchestrator, et shopify_billing route.Je lis les derniers fichiers critiques — orchestrateur, route fixes, auth, et shopify_billing service.J'ai tout ce qu'il me faut. Audit complet sur 12 fichiers critiques.

---

## ✅ AUDIT STOREMD — 26/04/2026

### 3 FIXES CONFIRMÉS ✅

**Fix 1 — Scopes OAuth** : `config.py` est correct.
```
read_products, write_products, read_themes, read_orders,
read_script_tags, write_script_tags,
read_url_redirects, write_url_redirects
```
Le commentaire de migration est présent. Les 5 mutations de `one_click_fixer.py` ont maintenant leurs scopes write correspondants. **Les fixes auto fonctionnent.**

**Fix 2 — Ghost billing** : `ghost_billing.py` est entièrement réécrit. Fini le REST déprécié — deux requêtes GraphQL propres, cross-référence logique, deep link `cancel_url` contextuel avec les étapes guidées. Code production-ready.

**Fix 3 — billing.py** : Route Stripe correcte avec `checkout`, `portal`, `usage`. `StripeBillingService` appelé proprement. **La conversion Starter/Pro/Agency est déblocquée.**

---

### 🔴 BUG CRITIQUE — Ghost billing : la cross-référence ne détecte rien

C'est le seul vrai problème qui reste. Voilà ce que fait le code :

```python
# Requête 1 — FETCH_BILLING_QUERY
appInstallations(first: 50)  → apps avec activeSubscriptions

# Requête 2 — FETCH_INSTALLED_QUERY  
appInstallations(first: 50)  → apps installées

# Cross-référence
if app["id"] in installed_app_ids: continue  # ghost charge ?
```

Les deux requêtes utilisent **exactement le même endpoint**, sans filtre de statut. Shopify retourne le même ensemble d'apps dans les deux cas. La cross-référence compare deux listes identiques → **0 ghost charges détectées en permanence**.

Pour que ça marche, il faudrait que la deuxième requête filtre sur `status: ACTIVE` (apps actuellement installées) pendant que la pr
