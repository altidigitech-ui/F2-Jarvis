---
tags: [fabrice, conversation, 2026-04-27]
date: "2026-04-27"
source: "jarvis-web"
messages: 2
---
# Session fabrice — 2026-04-27 21h15 (2 échanges)

[USER 21:10]
Alors ?

[ASSISTANT 21:10]
J'ai fait le tour complet de la stack. Voici le diagnostic :

**Ce qui existe déjà :**
- Backend Express : `limit: "25mb"` ✅
- Next.js proxy : `maxBodySize = "25mb"` + `bodySizeLimit: "25mb"` ✅
- Textarea UI : aucun `maxLength`, pas de limite côté input ✅

**Ce qui pose problème :**
1. **Historique des messages** → slicé à 2000 chars par message dans le system prompt (`chat.ts` ligne 93). Si tu colle un gros dump LinkedIn de 8000 chars, je n'en vois que 2000 dans les messages suivants.
2. **Fichiers attachés** → tronqués à 50k chars côté backend. Limite raisonnable mais perfectible.

**Fix proposé — 2 patches ciblés :**

- Historique : `slice(0, 2000)` → `slice(0, 6000)` — je garde 3× plus de contexte de tes longs messages
- Fichiers : `slice(0, 50000)` → `slice(0, 150000)` — 3× plus de contenu par fichier attachéDeux patches sur `chat.ts` — à valider :

**Patch 1 — historique messages : `2000` → `6000` chars**
Quand tu colles un gros dump LinkedIn/Twitter de 8000 caractères, je n'en gardais que les 2 premières pages en mémoire pour les messages suivants. Avec 6000 c'est 3× mieux — presque tout ton paste sera récupérable en contexte.

**Patch 2 — fichiers attachés : `50k` → `150k` chars**
Si tu drag & drop un `.txt` ou `.md` contenant des résultats de recherche, le backend envoie maintenant 3× plus de contenu à Claude. Utile pour les gros exports CSV ou rapports.

Ce qui ne change pas : les messages directs tapés dans la textarea n'ont aucune limite (25MB côté infra) — ça a toujours fonctionné. C'est uniquement le rappel contextuel et les fichiers qui étaient bridés.

[ACTION_PENDING:667457cb-f28d-4a14-92bc-eb93a647ef4b]

[TAG:Redeploy backend après validation]

---

[USER 21:12]
Justement j'envoie des message copier coller de tout les post et tu les voie pas tous en entier donc c'est les message direct qui doivent augmenter

[ASSISTANT 21:12]
C'est bien la ligne 93 — `slice(0, 2000)`. Quand ton paste de 5000+ chars passe en historique (le tour suivant), je n'en vois plus que les 2 premières "pages". C'est pour ça que les résultats LinkedIn/Twitter du tour précédent semblent tronqués.

Fix : `2000` → `15000` — ça couvre tes dumps complets confortablement sans exploser le context window.**1 patch, 1 ligne** — `slice(0, 2000)` → `slice(0, 15000)` dans `chat.ts`.

Ce qui change concrètement : quand tu colles un gros bloc de résultats (tes 5000+ chars de posts Twitter/LinkedIn), il reste visible en entier dans mon contexte aux tours suivants. Avec 2000 chars je voyais environ les 40 premières lignes — avec 15000 je vois tout.

Le patch précédent `[ACTION_PENDING:667457cb]` sur les fichiers attachés (50k → 150k) reste séparé. Si tu veux les 2, valide les deux.

[ACTION_PENDING:49a5d4c2-c79e-44f0-8511-ff0905bcfe25]

[TAG:Redeploy après validation] [TAG:Thread Mardi 13h draft à rédiger]
