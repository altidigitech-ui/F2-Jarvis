---
tags: [fabrice, conversation, 2026-04-26]
date: "2026-04-26"
source: "jarvis-web"
messages: 1
---
# Session fabrice — 2026-04-26 18h51 (1 échanges)

[USER 18:45]
Ok ben on va tout régler et faire tout ce que tu as dit tout doit etre parfait je vais faire sa avec Claude code il va falloir que tu me donne les prompt précis pour qu'il puisse coder tout sa sans problémes

[ASSISTANT 18:45]
Je relis les fichiers critiques de StoreMD en parallèle avant de te donner les prompts exacts.`billing.py` est correct — mon diagnostic de ce soir était basé sur un état antérieur, il a été fixé. Je relis les vraies failles maintenant.J'ai tout. Audit complet. Voilà la vraie liste des problèmes — différente de ce que j'avais dit ce soir (billing.py est correct, il a déjà été fixé). Les vrais blockers sont autres.

---

## ÉTAT RÉEL DE STOREMD — AUDIT 26/04

**Ce qui fonctionne :** `billing.py` ✅, `shopify_billing.py` ✅, `fixes.py` ✅, `orchestrator.py` ✅, `auth.py` ✅, `one_click_fixer.py` ✅ (code propre, 5 mutations GraphQL bien écrites)

**Ce qui bloque l'argent :**

### Blocker #1 — Toutes les fixes auto échouent silencieusement

`config.py` ligne 29 : `SHOPIFY_SCOPES = "read_products,read_themes,read_orders,read_script_tags"` — **zéro scope write**. Pourtant `one_click_fixer.py` exécute `productImageUpdate`, `metafieldsSet`, `productUpdate`, `scriptTagDelete`, `urlRedirectCreate`. Chaque appel reçoit une erreur Shopify 403 que Shopify retourne dans `userErrors[]` → `_raise_on_user_errors` lève une `ShopifyError` → la fix échoue. Le merchant voit un bouton "Fix" qui ne fait rien.

### Blocker #2 — Ghost billing (feature star du marketing) ne fonctionne jamais

`ghost_billing.py` ligne 37 : `await shopify.rest_get("recurring_application_charges")` — endpoint REST probablement inaccessible en API 2026-01 (déprécié). Catch générique → retourne une issue `severity="info"` invisible. Le scan affiche "check requires additional permissions" au lieu de vrais ghost charges.

### Blocker #3 — UX "fix manuel" pour ghost billing = mort de conversion

Même si la détection fonctionnait, `auto_fixable=False` + `fix_description="Cancel in Shopify Admin -> Settings -> Billing"` = experience nulle. Shopify ne permet pas à une app tierce d'annuler des charges — mais on peut générer un deep link direct + steps guidés dans le dashboard au lieu d'une string morte.

---

## 3 PROMPTS POU
