---
tags: [fabrice, conversation, 2026-04-26]
date: "2026-04-26"
source: "jarvis-web"
messages: 1
---
# Session fabrice — 2026-04-26 20h09 (1 échanges)

[USER 20:05]
Il faut régler les 2 point que tu as cité

[ASSISTANT 20:05]
Je lis les deux fichiers en parallèle.Ces fichiers sont dans le repo StoreMD, pas dans F2-Jarvis — je ne peux pas les lire directement. Je vais te donner les 2 prompts exacts pour Claude Code, basés sur mon audit.

---

## PROMPT 1 — Cosmétique : error code Shopify billing

```
You are working on STOREMD — a Shopify store diagnostic SaaS.

PROBLEM: In backend/app/api/routes/shopify_billing.py, around the /cancel
route (~line 90), there is a misleading error code:

    raise BillingError(code=ErrorCode.STRIPE_SUBSCRIPTION_NOT_FOUND, ...)

This is raised for a Shopify Billing operation, not a Stripe operation.
The enum VALUE is "BILLING_SUBSCRIPTION_NOT_FOUND" (correct and generic),
but the constant NAME is STRIPE_* which is misleading in Sentry logs.

WHAT TO DO:
1. Read backend/app/api/routes/shopify_billing.py
2. Read backend/app/exceptions.py to see the full ErrorCode enum

In exceptions.py, check if a constant named SHOPIFY_SUBSCRIPTION_NOT_FOUND
(or similar) already exists. If yes, use it. If not, add one:

    SHOPIFY_SUBSCRIPTION_NOT_FOUND = "BILLING_SUBSCRIPTION_NOT_FOUND"

Keep the VALUE exactly "BILLING_SUBSCRIPTION_NOT_FOUND" — do NOT change it.
Only the constant NAME changes so Sentry logs are readable.

3. In shopify_billing.py /cancel route, replace:
    ErrorCode.STRIPE_SUBSCRIPTION_NOT_FOUND
   with:
    ErrorCode.SHOPIFY_SUBSCRIPTION_NOT_FOUND

4. Search the entire codebase for any other BillingError raised in
shopify_billing.py that uses a STRIPE_* error code for a Shopify operation,
and apply the same rename pattern.

Do NOT change any error code VALUES. Only rename the Python constants.
Run existing tests after the change.
```

---

## PROMPT 2 — Edge case : redirect ID null → warning log

```
You are working on STOREMD — a Shopify store diagnostic SaaS.

PROBLEM: In backend/app/agent/actors/one_click_fixer.py, the apply_redirect()
method stores the created redirect ID in after_state so that delete_redirect()
can revert it later.

The current patt
