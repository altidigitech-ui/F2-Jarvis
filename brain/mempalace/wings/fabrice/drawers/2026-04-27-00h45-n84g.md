---
tags: [fabrice, conversation, 2026-04-27]
date: "2026-04-27"
source: "jarvis-web"
messages: 1
---
# Session fabrice — 2026-04-27 00h45 (1 échanges)

[USER 00:42]
C'est fait verifie

[ASSISTANT 00:42]
Propre à 100%. Tout est correct.

**`TimelineColumn.tsx` ligne 18** — `id="jarvis-timeline"` ✅

**`PersonaLayout.tsx` Desktop (ligne 772)** — une seule prop, ID correct :
```ts
onOpenToday={() => document.getElementById("jarvis-timeline")?.scrollTo({ top: 0, behavior: "smooth" })}
```

**`PersonaLayout.tsx` Mobile (ligne 703)** — une seule prop, propre :
```ts
onOpenToday={() => { setMobilePanel("timeline"); }}
```

Plus de doublon. Plus de `timeline-scroll-col` fantôme. Les 3 interfaces (Fabrice, Romain, F2) héritent du fix via le composant partagé. Le bouton "Aujourd'hui" fonctionne.

Bonne nuit — J1 S7 dans quelques heures.

[TAG:Thread manuel mardi 13h00] [TAG:Cross A1 14h05 demain]
