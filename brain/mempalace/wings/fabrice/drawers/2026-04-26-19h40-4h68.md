---
tags: [fabrice, conversation, 2026-04-26]
date: "2026-04-26"
source: "jarvis-web"
messages: 1
---
# Session fabrice — 2026-04-26 19h40 (1 échanges)

[USER 19:37]
Je veut regler tout les problémes meme petit il doit plus rien avoir

[ASSISTANT 19:37]
Je lis les 5 fichiers avec des bugs en parallèle — contenu exact avant de rédiger les prompts.Je lis les derniers fichiers dont j'ai besoin — exceptions, stripe, dependencies — pour avoir les infos exactes sur chaque bug.J'ai tout. 5 fichiers lus, bugs analysés en profondeur. Je génère maintenant les 4 prompts précis.

---

## AUDIT FINAL — 4 BUGS À CORRIGER

Voilà l'état réel après lecture des fichiers source :

| # | Sévérité | Fichier(s) | Bug |
|---|---|---|---|
| 1 | 🔴 CRITIQUE | `ghost_billing.py` | Cross-ref identique → 0 ghost jamais détecté |
| 2 | 🟠 MODÉRÉ | `one_click_fixer.py` + `fixes.py` | Revert redirect appelle `apply_redirect("","")` → Shopify rejette |
| 3 | 🟡 MINEUR | `shopify_billing.py` + `exceptions.py` | Toutes les erreurs Shopify billing utilisent `STRIPE_CHECKOUT_FAILED` |
| 4 | ✅ INFO | — | Pricing $29/$79/$199 → à vérifier vs ta page marketing (je ne peux pas confirmer) |

Bug #3 Supabase/Depends : **pas un vrai bug**. `get_supabase_service()` est un singleton factory synchrone → l'appel direct est fonctionnellement correct. Pas de prompt pour ça.

---

## PROMPT 1 — Ghost billing : `uninstalledAt` filter

```
You are working on STOREMD — a Shopify store diagnostic SaaS.

PROBLEM: The ghost billing scanner (backend/app/agent/analyzers/ghost_billing.py)
never detects any ghost charges. The bug is in the cross-reference logic.

ROOT CAUSE:
Both FETCH_BILLING_QUERY and FETCH_INSTALLED_QUERY use:
  appInstallations(first: 50)
with no filter. Shopify's appInstallations returns ALL historical records —
including apps that have been uninstalled. So both queries return the same
set of apps → cross-reference finds nothing → 0 ghost charges.

THE FIX:
In FETCH_INSTALLED_QUERY, add the "uninstalledAt" field so we can filter
to only currently-installed apps in Python.

Also update the Python set comprehension that builds installed_app_ids
to only include apps where uninstalledAt is None.

EXACT CHANGES:

1. In ghost_billing.py, replace FETCH_INSTAL
