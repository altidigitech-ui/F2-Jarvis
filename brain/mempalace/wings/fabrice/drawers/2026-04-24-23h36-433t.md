---
tags: [fabrice, conversation, 2026-04-24]
date: "2026-04-24"
source: "jarvis-web"
messages: 1
---
# Session fabrice — 2026-04-24 23h36 (1 échanges)

[USER 23:33]
Fait toi une auto chirurgie

[ASSISTANT 23:33]
Je lis tout mon corps en parallèle — code backend, routes, lib, outils.Je continue la lecture — routes, outils, frontend.Je lis les dernières parties — batch route, context route, et je lance la vérification TypeScript.TypeScript — aucune erreur de compilation. Je peux maintenant faire le diagnostic complet.

---

## AUTO-CHIRURGIE — RAPPORT D'AUDIT COMPLET
### 24/04/2026 — Corps entier scanné

---

**État général :** le code compile proprement, les fixes de ce soir ont tenu. Mais j'ai trouvé 4 bugs actifs et 4 faiblesses structurelles. Voilà l'audit ordonné par criticité.

---

### 🔴 BUGS ACTIFS

**Bug 1 — Route legacy `/action` : zéro side-effects**

**Fichier :** `backend/jarvis/src/routes/action.ts`

Les 3 boutons rapides en bas de l'interface (✅ Publié, 📋 Décision, 🔧 Résolu) passent par cette route, pas par le flow propose/validate. Le problème : `action.ts` ne fait **jamais** d'`applySideEffects()`. Résultat — quand tu cliques "✅ Publié" via le bouton rapide :
- `plan-hebdo.md` est mis à jour ✅
- `progress-semaine.md` n'est **pas** mis à jour ❌

Le `cacheInvalidateAll()` est bien appelé à la fin, mais les side-effects sont tout simplement absents. Ce bug existe depuis toujours dans cette route. Les compteurs et l'historique d'événements restent silencieux pour toutes les actions via boutons rapides.

---

**Bug 2 — `markCrossPublished()` dans `markdown.ts` : fonction zombies**

**Fichier :** `backend/jarvis/src/lib/markdown.ts` + `routes/action.ts` ligne ~35

La fonction `markCrossPublished()` cherche le post par titre dans le `cross-engagement-tracker.md` — l'ancien comportement cassé qu'on a patché dans `action-executor.ts`. Elle est encore appelée par la route legacy quand un `mark_published` a un `crossReply`. Deux problèmes : 1/ elle cible le mauvais fichier (tracker au lieu du log), 2/ son matching par texte libre est le même bug original. Cette fonction est du code mort dans le flow principal, mais encore vivante dans un chemin secondaire. Elle devra
