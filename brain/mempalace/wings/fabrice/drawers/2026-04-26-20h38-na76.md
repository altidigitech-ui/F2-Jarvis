---
tags: [fabrice, conversation, 2026-04-26]
date: "2026-04-26"
source: "jarvis-web"
messages: 1
---
# Session fabrice — 2026-04-26 20h38 (1 échanges)

[USER 20:35]
Je peut pas scanner sur ma landing page je comprend pas comment je peut scanner enfaite et j'ai deja une boutique test on s'en fou faut tester des vrais boutique et la en l'etat je peut pas l'App est mal faite quelque pars

[ASSISTANT 20:35]
J'ai tout. Le diagnostic est précis — **double blocage sur la regex**, et rien d'autre. Le reste du code est propre.

---

## CE QUI SE PASSE EXACTEMENT

Le `runner.py` fait juste `https://{shop_domain}` et crawle la page. Il fonctionne avec n'importe quelle URL. **Le preview scan peut scanner `gymshark.com` sans problème.** Les 6 checkers (SEO, Accessibility, Security, Performance, Links, Robots) ne nécessitent aucun OAuth.

Le seul problème : **deux regex identiques bloquent tout avant d'arriver au scanner**.

**Blocage 1 — Frontend `ShopInputModal.tsx` ligne 7 :**
```javascript
const SHOP_REGEX = /^[a-zA-Z0-9][a-zA-Z0-9-]*\.myshopify\.com$/;
```
`gymshark.com` a un point → `normalizeShopDomain` le retourne as-is → la regex échoue → affiche l'erreur rouge.

**Blocage 2 — Backend `preview.py` ligne 14 :**
```python
_SHOP_DOMAIN_RE = re.compile(r"^[a-zA-Z0-9][a-zA-Z0-9\-]*\.myshopify\.com$")
```
Même chose — même si le frontend passe, le backend rejette.

**Blocage bonus — `normalizeShopDomain` ne strip pas `https://`**. Si tu copieras-colles `https://gymshark.com`, ça passerait le point check mais garderait le protocole dans l'URL → le backend recevrait `https://gymshark.com` au lieu de `gymshark.com`.

---

## 2 PROMPTS POUR CLAUDE CODE

### PROMPT 1 — Frontend : `ShopInputModal.tsx`

```
You are working on STOREMD frontend (Next.js).

FILE: frontend/src/components/landing/ShopInputModal.tsx

PROBLEM: The modal only accepts .myshopify.com domains. Merchants and admins
need to be able to enter custom domains like gymshark.com, allbirds.com, etc.
The backend preview scanner (runner.py) works with any domain — it just does
https://{domain} and crawls it. The only blocker is the frontend validation.

CHANGE 1 — Replace the SHOP_REGEX constant (line 7):

CURRENT:
const SHOP_REGEX = /^[a-zA-Z0-9][a-zA-Z0-9-]*\.myshopify\.com$/;

NEW:
const SHOP_REGEX = /^[a-zA-Z0-9][a-zA-Z0-9.-]*\.[a-zA-Z]{2,}$/;

This accepts both gymshark.myshopify.com AND gymshark.com AND shop.gymsha
