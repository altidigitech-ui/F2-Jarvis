---
tags: [fabrice, conversation, 2026-04-27]
date: "2026-04-27"
source: "jarvis-web"
messages: 1
---
# Session fabrice — 2026-04-27 12h55 (1 échanges)

[USER 12:52]
Repond

[ASSISTANT 12:52]
Tout lu. Diagnostic complet — voilà exactement ce qui se passe.**Bug identifié — cause racine double.**

**Ce qui se passe exactement :** quand JARVIS termine ses tool calls (tous ✓ dans la bulle), il envoie les résultats à Claude API pour générer la réponse textuelle. Si Claude API est lent ou figé à ce stade, le stream reste ouvert indéfiniment — les pings keepalive (toutes les 8s) arrivent bien, donc la connexion semble vivante, mais `reader.read()` n'avance plus. `isStreaming` reste `true`. Ton prochain message est silencieusement bloqué par le guard `if (isStreaming) return`. Tu dois rafraîchir.

Le screenshot confirme : 6 tool calls complétés à 12:36, screenshot à 12:44 = 8 minutes sans réponse textuelle. Connection alive (pings reçus), Claude figé.

**Le fix : deux watchdogs dans `send()`**
- **Ping watchdog** (25s sans aucun octet) → abort = connexion morte
- **Response watchdog** (90s sans `text` ou `tool_use`) → abort = Claude figé après tool calls, message d'erreur affiché**4 patches, 1 fichier, bug tué.**

---

**Ce qui causait le problème (les 3 chats identiques)**

JARVIS faisait ses tool calls (tous ✓), envoyait les résultats à Claude API, et là Claude API prenait un temps anormal pour générer la réponse textuelle. La connexion restait vivante (pings toutes les 8s reçus), mais `reader.read()` ne recevait plus rien de substantiel. `isStreaming` restait `true` à l'infini. Ton prochain message touchait le guard `if (isStreaming) return` → silencieusement ignoré. Seul le refresh résolvait.

**Ce que le fix apporte**

- **Watchdog ping (25s)** — si aucun octet en 25s (3 pings manqués), la connexion est morte → abort propre → `isStreaming` reset → tu peux renvoyer.
- **Watchdog réponse (90s)** — si des pings arrivent (connexion vivante) mais aucun `text` ni `tool_use` depuis 90s → Claude est figé après ses tool calls → abort + message ⏱️ dans la bulle → tu sais exactement quoi faire.
- **Cleanup dans `finally`** — les deux `clearTimeout` sont appelés dans t
