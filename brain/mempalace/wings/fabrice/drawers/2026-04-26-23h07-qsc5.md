---
tags: [fabrice, conversation, 2026-04-26]
date: "2026-04-26"
source: "jarvis-web"
messages: 2
---
# Session fabrice — 2026-04-26 23h07 (2 échanges)

[USER 23:03]
Parfait maintenant j'ai un probléme d'interface a gauche dans accés rapide il y'a un bouton Aujourd'hui mais quand je clique dessus rien ne s'affiche regarde pour les 3 interface Fabrice Romain F2

[ASSISTANT 23:03]
Diagnostic clair. Le bug est identique sur les 3 interfaces — voilà ce qui se passe exactement.

**Le problème en 3 lignes**

Dans `PersonaLayout.tsx`, les deux instances de `QuickAccessSidebar` (desktop ligne 767 et mobile ligne 699) ne passent **jamais** la prop `onOpenToday`. Le fallback dans `QuickAccessSidebar.tsx` est `window.scrollTo({ top: 0 })` — mais le layout est en `overflow-hidden`, donc le body ne scroll pas. Résultat : rien ne se passe visuellement.

**La cause racine**

`onOpenToday` n'a jamais été câblé dans PersonaLayout. Le prop existe dans QuickAccessSidebar, mais il n'est passé nulle part.

**Le fix : 2 patches**

**Patch 1 — `TimelineColumn.tsx`** : ajouter `id="jarvis-timeline"` sur le `<aside>` pour pouvoir le cibler depuis PersonaLayout.

**Patch 2 — `PersonaLayout.tsx`** :
- Desktop QuickAccessSidebar → `onOpenToday` scrolle le `<aside id="jarvis-timeline">` en haut (le planning du jour est toujours visible mais peut avoir scrollé)
- Mobile QuickAccessSidebar → `onOpenToday` appelle `setMobilePanel("timeline")` pour ouvrir le panneau timeline mobile qui existait déjà mais n'était jamais déclenché depuis "Aujourd'hui"2 patches, 2 fichiers. Indépendants — valide-les dans l'ordre que tu veux.

**Patch 1 — `TimelineColumn.tsx`**
Un seul mot ajouté : `id="jarvis-timeline"` sur le `<aside>`. Ça donne une cible stable pour le scroll sans toucher à la logique du composant.

[ACTION_PENDING:7d3fede1-917b-4bb2-af09-6ca56ca80172]

**Patch 2 — `PersonaLayout.tsx`**
Les deux instances de `QuickAccessSidebar` reçoivent enfin `onOpenToday` :
- **Desktop** → `document.getElementById("jarvis-timeline")?.scrollTo({ top: 0 })` — ramène le planning du jour en haut si tu avais scrollé
- **Mobile** → `setMobilePanel("timeline")` — ouvre le drawer timeline qui existait déjà mais n'était jamais déclenché depuis ce bouton

Ça couvre les 3 interfaces (Fabrice, Romain, F2) parce que les deux instances de QuickAccessSidebar sont partagées par toutes les personas.

[AC

---

[USER 23:04]
Parfait maintenant j'ai un probléme d'interface a gauche dans accés rapide il y'a un bouton Aujourd'hui mais quand je clique dessus rien ne s'affiche regarde pour les 3 interface Fabrice Romain F2

[ASSISTANT 23:04]
Le diagnostic est clair. **Double cause, 2 fichiers à patcher.**

**`TimelineColumn.tsx`** — L'`<aside>` scrollable n'a pas d'identifiant pour être ciblé.  
**`PersonaLayout.tsx`** — `onOpenToday` n'est passé à **aucun** des deux `QuickAccessSidebar` (ni desktop ni mobile). Le fallback `window.scrollTo({ top: 0 })` ne fait rien parce que la page est en `overflow-hidden`.

Fix : je donne un `id` à l'aside, puis je câble les deux handlers — desktop : scroll en haut de la timeline, mobile : ouvre le panel timeline.2 patches, 2 fichiers.

**Patch 1 — `TimelineColumn.tsx`**
Ajoute `id="timeline-scroll-col"` sur l'`<aside>` — sans ça, impossible de le cibler depuis `PersonaLayout`.

**Patch 2 — `PersonaLayout.tsx`**
Câble `onOpenToday` sur les deux instances de `QuickAccessSidebar` :
- **Desktop** → `document.getElementById("timeline-scroll-col")?.scrollTo({ top: 0 })` — scrolle la colonne timeline en haut (utile si tu as scrollé dans le planning du jour)
- **Mobile** → `setMobilePanel("timeline")` — ouvre l'overlay timeline comme le fait le menu hamburger

Le F2 n'a pas de sidebar propre — il passe par Romain, donc il hérite du fix automatiquement.

[ACTION_PENDING:d941fa21-5719-4ce6-85a6-96543589a329]
[ACTION_PENDING:504a799e-567c-451d-a15f-f3f53cf2549d]

[TAG:Vercel redéploie ~1min] [TAG:J1 S7 demain 13h00]
