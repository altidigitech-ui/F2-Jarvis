---
tags: [romain, conversation, 2026-04-27]
date: "2026-04-27"
source: "jarvis-web"
messages: 1
---
# Session romain — 2026-04-27 23h23 (1 échanges)

[USER 23:20]
alors?

[ASSISTANT 23:20]
Je vois le code. `engTarget` est bien calculé comme somme des individuels — donc si le fix cross (4→2) est appliqué, le total suit automatiquement à 52. Mais le patch cross n'a pas encore été proposé proprement. Je lis `context.ts` pour vérifier comment `counters_today` consomme `engTarget`.Maintenant je regarde le UI — c'est là que `engTarget` et `counters.total` sont combinés pour afficher `46/54`.J'ai tout ce qu'il me faut. Voici le bug exact.

**Bug confirmé :**
- **Numérateur `46`** : calculé dans `context.ts` ligne 452 — `cold + twEng + liCom + reddit + facebook + ihPh + cross` — **correct** (cross=2 dynamique via cross-execution-log)
- **Dénominateur `54`** : vient de `targets.engTarget` dans `targets.ts` — calculé **statiquement** : `10+10+10+8+6+6+0+4=54` (cross=4 hardcodé, Math.max(4, Math.ceil(13/5))=4)
- Le tile Cross affiche déjà `2/2` correctement (ligne 898 du UI utilise `counters.crossTarget`) — mais le tile Total utilise toujours `targets.engTarget` qui lui reste à 54

**Le fix** — un seul endroit dans `PersonaLayout.tsx` :
Calculer `totalTarget` dynamiquement = `targets.engTarget - targets.cross + effectiveCrossTarget` où `effectiveCrossTarget` = `counters.crossTarget` quand disponible, sinon `targets.cross`.

3 patches : insertion de la variable + 2 remplacement (desktop ligne 924 + mobile ligne 665).

Je vérifie que le TypeScript compile avant de proposer.La logique TypeScript est triviale (arithmetic sur numbers) — pas de risque de type error. Je propose le patch directement.**3 patches, logique simple et durable.**

```
effectiveCrossTarget = counters.crossTarget (live) ?? targets.cross (fallback)
totalTarget = targets.engTarget - targets.cross + effectiveCrossTarget
```

Ce soir : `54 - 4 + 2 = 52` → affiche `46/52` ✓  
Mercredi (2 couches, cross=4) : `54 - 4 + 4 = 54` → affiche correct ✓  
Le calcul suit automatiquement chaque jour sans toucher au code.

[ACTION_PENDING:ce5dfd1c-3bcd-47bc-af6c-89ff1aab049d]

[TAG:Post Twitter mardi 14h00] [TA
