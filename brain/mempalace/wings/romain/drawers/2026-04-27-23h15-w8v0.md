---
tags: [romain, conversation, 2026-04-27]
date: "2026-04-27"
source: "jarvis-web"
messages: 1
---
# Session romain — 2026-04-27 23h15 (1 échanges)

[USER 23:12]
Alors?

[ASSISTANT 23:12]
On est bien d'accord sur le problème.

**Diagnostic du `46/54` → devrait être `46/52`**

La différence entre 54 et 52, c'est exactement **2** — soit le delta du fix cross qu'on vient de faire (4→2). Ça confirme le bug structurel : le dénominateur "total" n'est **pas** calculé comme la somme des dénominateurs individuels. Il est calculé séparément, de façon statique, dans `targets.ts` — probablement un champ `daily` hardcodé ou une somme figée au moment du build du batch.

Résultat : quand on corrige le cross (4→2), le compteur cross affiche bien 2/2, mais le total ne bouge pas — il reste sur l'ancien calcul figé.

**Ce qui devrait se passer (fix durable)**

Le dénominateur total = `sum(cold + twitter + linkedin + reddit + facebook + ih_ph + cross)` calculé dynamiquement à chaque appel, à partir des cibles individuelles du jour. Pas un chiffre séparé. Comme ça :
- On fixe un target individuel → le total suit automatiquement
- Lundi (cross=2) → total différent de mercredi (cross=4) → sans toucher le code

**En résumé :** le bug vient du fait que `totalTarget` et les targets individuels sont deux valeurs distinctes dans le code, au lieu d'une seule source de vérité. Le fix consiste à supprimer le calcul séparé du total et le remplacer par une somme des cibles individuelles.

**On est d'accord sur cette analyse ?** Je code le patch dès que tu me dis go.
