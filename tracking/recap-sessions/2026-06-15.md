# RECAP SESSION — 15/06/2026 — LA MACHINE F2 (cold acquisition + moteur réseaux) — Phases 1 à 8

> Session marathon de build. Construction quasi complète de **La Machine F2** (acquisition autonome StoreMD) dans `backend/jarvis/`, en suivant `marketing/saas-app-shopify/storemd/machine/plan.md`.
> Phase RECO → puis Phases 1 à 8 du plan, livrées en **6 PRs stacked** (#328 → #337), **aucune mergée** (en attente de validation).
> En amont : diagnostic des erreurs `/doctor` (non corrigées) et tentatives `/handoff` (rejetées).
> **Règle de session :** branches + PR, jamais de commit direct sur main ; `.gitignore` (modif pré-existante non liée) volontairement exclu de toutes les PRs.

---

## CONTEXTE

- Point de départ : diagnostic `/doctor` sur `.claude/settings.json`, puis bascule sur le vrai chantier — bâtir la machine cold + réseaux de StoreMD décrite dans `machine/` (context.md + plan.md + 00-RECO.md).
- Tout le code vit dans **`backend/jarvis/`** (Express + BullMQ + Supabase service-role + Redis), destiné à tourner sur 1 VPS avec OpenClaw. Conventions respectées : ESM imports `.js`, REST direct Haiku pour la génération, migrations SQL à la racine `supabase-migrations/`, écriture repo via l'API GitHub (`lib/github.ts`).
- `STOREMD/backend/` n'est PAS dans ce repo (submodule `saas/storemd` commenté). Le contrat scan a été confirmé via Supabase, pas par lecture du code.

---

## ACTÉ EN SYNTHÈSE (décisions clés)

1. **Contrat scan StoreMD confirmé via Supabase** (Tâche B du RECO levée) : le scan tourne sur n'importe quelle `store_url` (gymshark testé, pas besoin de shop installé) ; `preview_scans` ne stocke que score + compteurs ; les **findings détaillés viennent de la réponse de l'endpoint interne** StoreMD.
2. **Fournisseur cold = Maildoso** (creds SMTP+IMAP bruts exportables, utilisables par code). Mailforge en option budget. Zapmail écarté (Google Workspace + API derrière tier Pro 299$).
3. **Pipeline 100% déterministe côté Jarvis ; OpenClaw = couche browser** (découverte d'URLs + email décideur), via Gateway HTTP. Jarvis ne fait pas tourner le browser lui-même.
4. **Scan câblé en synchrone** (`POST /internal/preview-scan`, auth Bearer) avec parser tolérant aux variantes de noms de champs (contrat exact non documenté dans le repo).
5. **Logging = fichiers EXISTANTS, pas de nouveau système** (plan §8) : agrégé dans Git, nominatif dans Supabase, jamais de PII ni d'envois bruts. Cycle réutilise le cron `/api/commit-batch` existant (pas de cron parallèle).
6. **Garde-fous délivrabilité** : bounce > 2% ou plaintes > 0,3% → pause auto (flag Redis + stop envois + stop sourcing + ligne `decisions-log.md`).
7. **Réseaux : seul X est activé.** IG/FB/TikTok/LinkedIn volontairement non codés (`PublisherNotEnabledError`) car approbations longues + `04-RESEAUX-API.md` interdit de coder les endpoints sans les reconfirmer. L'humain valide toujours avant publication.
8. **ANTI-IA (§0) appliqué à tout le contenu généré** : sujet cold templaté sans em-dash (`domain: score/100`), corps Haiku contraint + garde-fous (em-dash neutralisé, opt-out forcé), captions réseaux idem.

---

## CHANTIERS RÉALISÉS

### 0. Diagnostic `/doctor` sur `.claude/settings.json` [DIAGNOSTIQUÉ — NON corrigé]
9 erreurs identifiées (model objet au lieu de string ; hooks au mauvais format `{matcher, hooks:[]}` ; statusLine sans `type:"command"`). Plan de correction présenté. **Rien écrit** : décision sur statusLine restée en attente (supprimer / commande / laisser), puis la session a basculé sur la machine. `settings.json` inchangé.

### 0bis. Tentatives `/handoff` [ABANDONNÉ]
Deux écritures de `HANDOFF.md` rejetées par l'utilisateur, puis "stop". `HANDOFF.md` (daté 26/04, S7) **inchangé**.

### 1. Phase RECO → `reco-findings.md` [FAIT — non versionné séparément, vit dans machine/]
Audit (A) conventions Jarvis, (B) contrat scan StoreMD, (C) fournisseur cold, (D) dépendances API à lead-time long. Tâche B mise à jour après confirmation Supabase (passée de flag rouge à confirmée).

### 2. PR #328 — Phase 1 : data model [PRÊT — poussé, non mergé]
`supabase-migrations/003_cold_targets.sql` : tables `cold_targets` (13 statuts en check, unique `store_domain`, index status + next_touch_at) + `cold_suppression`. RLS service-role, trigger updated_at, `NOTIFY pgrst`. + `reco-findings.md`.
Branche `feat/cold-machine-phase1-cold-targets`.

### 3. PR #329 — Phases 2-4 : séquenceur + scraping + jobs [PRÊT — poussé, non mergé]
Tout dans `backend/jarvis/src/lib/cold/` :
- **Phase 2** : `mailer.ts` (nodemailer, round-robin, cap/jour/boîte Redis, List-Unsubscribe), `imap.ts` (imapflow), `sequence.ts` (J0/J3/J7/J15), `mailboxes.ts`.
- **Phase 3** : `openclaw.ts` (Gateway), `shopify-detect.ts` (détection déterministe via /products.json + signatures), `smtp-verify.ts` (MX→RCPT sans envoyer + drop role), `scraper.ts` (SOURCE + ENRICH).
- **Phase 4** : `jobs.ts` (qualify, enrich, scan-STUB, compose Haiku, push) + crons sequence-tick/imap-poll. Wiring `queues.ts` + `worker.ts` + `server.ts`. Deps nodemailer + imapflow.
Branche `feat/cold-machine-phase2-4-pipeline` (base #328).

### 4. PR #332 — Job scan réel [PRÊT — poussé, non mergé]
`lib/cold/storemd.ts` (client preview-scan + `topFindings` = 3 plus parlants : sévérité puis chiffre d'impact). `jobScan` remplace le stub (enriched → scanned), chaînage worker complété `enrich → scan → compose`.
Branche `feat/cold-machine-scan-job` (base #329).

### 5. PR #334 — Bloc 4 : logging (Phase 8) + garde-fous (Phase 7) [PRÊT — poussé, non mergé]
- `guardrails.ts` : seuils bounce/plaintes → pause auto (Redis), `evaluateGuardrails()` au poll IMAP, détection bounces via DSN.
- `cold-log.ts` : append `cold-log-email.md` + `decisions-log.md` (API GitHub).
- `routes/cold.ts` (`GET /cold/cycle-log`) + extension du cron Vercel `commit-batch` pour la ligne cycle dans `batch-log.md`.
Branche `feat/cold-machine-bloc4-logging-guardrails` (base #332).

### 6. PR #336 — Phase 5 : LISTEN (classification) [PRÊT — poussé, non mergé]
`classify.ts` (Haiku → interested/objection/not_now/unsubscribe), `notify.ts` (webhook best-effort), `appendConversionPipeline()` (route interested → `pipeline-conversion.md`, insertion ciblée sous `## PIPELINE ACTIF`, sans PII). imap-poll branche la classification.
Branche `feat/cold-machine-phase5-listen` (base #334).

### 7. PR #337 — Phase 6 : moteur réseaux [PRÊT — poussé, non mergé]
`supabase-migrations/004_social_posts.sql` + `lib/social/` (types, generate Haiku par plateforme, publishers/x réel + dispatcher, jobs generate/publish) + `routes/social.ts` (pending/approve/reject) + wiring queue/worker/server (crons gardés par `enabledPlatforms()`).
Branche `feat/cold-machine-phase6-social` (base #336).

---

## À FAIRE (points de reprise — ordre)

1. **Merger les 6 PRs dans l'ordre** : #328 → #329 → #332 → #334 → #336 → #337 (chacune stacked sur la précédente).
2. **Appliquer les migrations** `003_cold_targets.sql` puis `004_social_posts.sql` dans Supabase (manuel, comme 001/002), `NOTIFY pgrst` inclus.
3. **Confirmer le contrat de l'endpoint interne StoreMD** (header d'auth exact, schéma de la réponse, sync vs async) → ajuster `lib/cold/storemd.ts` si besoin.
4. **Activation infra (Phase 0 humaine)** : VPS + souscription Maildoso (~10 boîtes sur leakdetector.tech), remplir les `MAILBOX_*`, `STOREMD_PREVIEW_SCAN_*`, `OPENCLAW_*`, `X_ACCESS_TOKEN`.
5. **Demandes API réseaux dès que possible** (lead-time long) : Meta, TikTok, LinkedIn. X branché en premier.
6. **Reprendre le fix `/doctor`** sur `.claude/settings.json` (model/hooks/statusLine) si souhaité — toujours non corrigé.

---

## RESTE OUVERT / DÉPENDANCES

- **Réseaux** : brancher IG/FB/TikTok/LinkedIn au fil des approbations (publishers à coder après reconfirmation des endpoints, cf. `04-RESEAUX-API.md`) ; refresh token X ; AI-labeling TikTok/Meta à implémenter à ce moment.
- **Plaintes fiables** : nécessitent un FBL (la détection actuelle est heuristique).
- **Média réseaux** : visuels via Higgsfield/Canva (côté Claude, hors backend) ; un post image/vidéo sans `media_url` est marqué `failed`.
- **UI de validation des posts** (cockpit `ui/jarvis`) : non construite, endpoints seulement. À ajouter si voulu.

---

## RÈGLES ÉTABLIES / RAPPELS

- **Pas de code à l'aveugle sur les API réseaux** : `04-RESEAUX-API.md` impose de reconfirmer endpoints/versions au build. D'où les publishers non-X en stub explicite.
- **Agrégé dans Git, nominatif dans Supabase** : jamais d'email de prospect ni d'envoi brut commité. Logs = Store URL + score + statut.
- **L'humain valide toujours** avant tout envoi cold et toute publication réseau.
- **ANTI-IA (§0) prime** sur tout contenu généré (cold + réseaux).
- **Ciblage US/UK/AU uniquement** (filtre SOURCE), pas d'EU/FR.

---

## PRs DE LA SESSION (toutes ouvertes, non mergées)

| PR | Phase | Branche |
|----|-------|---------|
| #328 | 1 — data model (cold_targets) | `feat/cold-machine-phase1-cold-targets` |
| #329 | 2-4 — séquenceur + scraping + jobs | `feat/cold-machine-phase2-4-pipeline` |
| #332 | scan réel (endpoint StoreMD) | `feat/cold-machine-scan-job` |
| #334 | 7-8 — logging + garde-fous | `feat/cold-machine-bloc4-logging-guardrails` |
| #336 | 5 — LISTEN (classification) | `feat/cold-machine-phase5-listen` |
| #337 | 6 — moteur réseaux | `feat/cold-machine-phase6-social` |

---

## CHEMINS REPO CRITIQUES (état 15/06/2026)

- Spec machine : `marketing/saas-app-shopify/storemd/machine/` (context.md, plan.md, 00-RECO.md, reco-findings.md, 01→05)
- Code cold : `backend/jarvis/src/lib/cold/` (+ `README.md`)
- Code réseaux : `backend/jarvis/src/lib/social/` (+ `README.md`)
- Routes : `backend/jarvis/src/routes/cold.ts`, `social.ts`
- Migrations : `supabase-migrations/003_cold_targets.sql`, `004_social_posts.sql`
- Logs cibles : `tracking/batch-log.md`, `tracking/decisions-log.md`, `marketing/saas-app-shopify/storemd/cold/cold-log-email.md`, `marketing/saas-app-shopify/storemd/pipeline-conversion.md`
- Cron existant réutilisé : `ui/jarvis/app/api/commit-batch/route.ts`

---

*Fin du recap session 15/06/2026. Reprise : merger les 6 PRs dans l'ordre, appliquer les migrations, confirmer le contrat endpoint StoreMD, puis activation infra (VPS + Maildoso + tokens).*
