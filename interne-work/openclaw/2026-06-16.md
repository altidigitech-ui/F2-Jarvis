# Recap session — 2026-06-16

> Suivi de session. On a suivi les étapes du fichier `machine/07-GUIDE-EXECUTION.md` (Lot 1).
> Le détail technique OpenClaw (2 clés manquantes + branchement) est dans un fichier dédié : `machine/08-OPENCLAW-BRANCHEMENT.md`.

---

## 1. Objet de la session
Démarrage de l'infra manuelle du Lot 1 (cold email) en suivant le guide `07`. Création de l'infra Maildoso, remplissage des variables Railway, et cadrage du branchement OpenClaw (scraping) qui reste à construire.

---

## 2. Suivi du guide 07 — Lot 1 (cold email)

### Maildoso — FAIT
- Compte créé, **plan 10 boîtes payé**.
- Domaine **`leakdetector.tech`** connecté via "Connecter les domaines existants" (import CSV à 1 colonne, 1 domaine, sans en-tête).
- **10 boîtes SMTP** créées (type Maildoso SMTP, pas Google Workspace) :
  `m.jones`, `j.cole`, `r.tunderlane`, `t.philips`, `d.cohen`, `k.douglas`, `j.smith`, `k.collins`, `p.farnes`, `p.deen` @leakdetector.tech.
- **Boîte de réception principale** créée : `reply@maildoso.email` (centralise les réponses des 10 boîtes pour lecture humaine ; n'empêche pas Jarvis de lire chaque boîte en IMAP).
- **CSV exporté** au format **Instantly** (contient SMTP + IMAP host/port/user/pass par boîte).

### Paramètres techniques confirmés (depuis le CSV Maildoso)
- SMTP : `smtp.maildoso.com` port `587`
- IMAP : `imap.hesperus.maildoso.com` port `993`
- Mot de passe **identique SMTP et IMAP**, et identique pour les 10 boîtes (normal chez Maildoso).
- **Warmup actif** : `Daily Limit = 3`, `Warmup Enabled = TRUE`, `Warmup Limit = 80`, `Warmup Increment = 4`.
  → Le débit réel est bridé par Maildoso (~3 envois/jour/boîte au départ, +4/jour jusqu'à 80). **Ne pas forcer le volume avant ~2 semaines.**

### Railway (projet Jarvis) — variables
- **Générées et fournies** (bloc Raw Editor) pour les **10 boîtes** : `MAILBOX_1..10_{SMTP_HOST,SMTP_PORT,SMTP_USER,SMTP_PASS,IMAP_HOST,IMAP_PORT,FROM}`.
- À coller au même endroit : `STOREMD_PREVIEW_SCAN_URL`, `STOREMD_PREVIEW_SCAN_KEY` (= `INTERNAL_SCAN_KEY`), `ANTHROPIC_API_KEY`, `COLD_DAILY_CAP_PER_INBOX=12`, `COLD_SMTP_PROBE_FROM` (une adresse @leakdetector.tech).
- **`OPENCLAW_GATEWAY_URL` et `OPENCLAW_API_KEY` laissées VIDES volontairement** — en attente du branchement WebSocket (cf. fichier 08). Le code gère l'absence sans planter.

### Reste à faire sur le Lot 1
- [ ] DNS Porkbun : déléguer les **nameservers** de `leakdetector.tech` vers ceux fournis par Maildoso (≤ 24h de propagation, activation ~2h après).
- [ ] Rédiger la **LIA** (1 page, B2B US/UK/AU).
- [ ] **Test bout-en-bout sur 1 boutique** (prospect inséré manuellement dans `cold_targets`) : scan → compose → envoi → relance. Vérif live Supabase.
- [ ] Brancher OpenClaw (les 2 clés) → **fichier 08**.

---

## 3. Décision actée
**Le scraping (SOURCE + ENRICH) passe entièrement par OpenClaw.** Branchement retenu : **Option B — Jarvis parle directement au Gateway OpenClaw en WebSocket (`wss://`)**.
- Raison : choix explicite (tout automatiser via OpenClaw, pas de service intermédiaire).
- Risque assumé : le protocole WebSocket d'OpenClaw n'est pas documenté pour cet usage → à extraire d'OpenClaw avant tout code, et à re-vérifier à chaque mise à jour d'OpenClaw.
- **Tout le détail actionnel/fonctionnel/opérationnel est dans `machine/08-OPENCLAW-BRANCHEMENT.md`.**

---

## 4. État infra global (rappel, hors session du jour)
- ✅ Endpoint interne StoreMD `POST /api/v1/internal/preview-scan` live + testé (gymshark 61/100, renvoie les findings détaillés).
- ✅ `storemd.ts` corrigé (contrat scan aligné) — PR #332, commit 7c1e739.
- ✅ Les 6 PRs de la machine mergées dans `main` (#328 → #337).
- ✅ Migration `003_cold_targets.sql` **appliquée** dans la base Jarvis (`hcgtxgnvqkalpbcroihx`) — `cold_targets` + `cold_suppression` prêtes (vérifiées : 21 colonnes, index, contrainte unique, trigger, RLS).
- ⏳ Migration `004_social_posts.sql` non appliquée (Lot 2).

---

## 5. Prochaines actions (ordre)
1. **Poser à OpenClaw les 4 blocs de questions** du fichier 08 → remonter les réponses.
2. Compléter la spec WebSocket (fichier 08) avec le protocole réel obtenu.
3. **Claude Code** réécrit la couche transport de `openclaw.ts` (Option B) → branche + PR (ne pas merger sans relecture).
4. En parallèle (indépendant d'OpenClaw) : **DNS Porkbun** + **LIA** + **test email** sur 1 boutique manuelle.
5. Renseigner alors `OPENCLAW_GATEWAY_URL` + `OPENCLAW_API_KEY` dans Railway et tester SOURCE/ENRICH.

---

## 6. Points de vigilance
- **Warmup** : volume réel bridé ~2 semaines. Configurer ≠ envoyer à fond le jour 1.
- **Secrets** (mots de passe boîtes, clés) : uniquement dans Railway. Jamais repo / chat / capture.
- **Protocole WebSocket OpenClaw non documenté** : ne pas coder le client avant d'avoir les réponses d'OpenClaw (fichier 08).
